{
  "name": "rcon-proxy",
  "version": "1.0.0",
  "description": "Plain ws:// RCON proxy for Rust Console Edition (GPortal) — Bun runtime",
  "main": "server.js",
  "scripts": {
    "start": "bun server.js"
  }
}
